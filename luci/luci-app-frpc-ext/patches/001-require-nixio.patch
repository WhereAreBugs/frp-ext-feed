From 8da6f7d1234567890abcdef1234567890abcdef Mon Sep 17 00:00:00 2001
From: cat <cat@example.com>
Date: Thu, 1 Jan 1970 00:00:00 +0000
Subject: [PATCH] luci-app-frpc-ext: require nixio for file checks

LuCI controllers reference nixio.fs, so load the module explicitly to
avoid a nil access and ensure the menu entry registers correctly after
installation.

--- a/luasrc/controller/frpc_ext.lua
+++ b/luasrc/controller/frpc_ext.lua
@@
-local http = require "luci.http"
-local uci = require "luci.model.uci".cursor()
-local sys = require "luci.sys"
+local http = require "luci.http"
+local uci = require "luci.model.uci".cursor()
+local sys = require "luci.sys"
+local nixio = require "nixio"
